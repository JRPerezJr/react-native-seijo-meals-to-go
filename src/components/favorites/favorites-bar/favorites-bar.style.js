import { Card } from 'react-native-paper';
import styled from 'styled-components';

export const FavoritesWrapper = styled(Card).attrs({
  elevation: 3,
})`
  padding: 10px;
  z-index: 999;
  border-radius: 15px;
`;
